// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-nocheck

/**
 *
 * @param {number} id
 * @param {string} name
 * @param {number} salary
 * @returns
 */
export function getEmployee(id, name, salary) {
  return {
    id,
    name,
    salary,
  };
}
